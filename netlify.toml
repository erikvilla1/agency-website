[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — react-router handles all routes client-side
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
